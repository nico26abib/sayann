# 📜 Scripts

## setup.sh
Installation complète (dépendances + Playwright + .env)

```bash
./scripts/setup.sh
```

## restart_bot.sh
Redémarre le bot (tue le processus + relance)

```bash
./scripts/restart_bot.sh
```

## Usage

Tous les scripts sont exécutables:
```bash
chmod +x scripts/*.sh
```

